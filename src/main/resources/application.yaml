spring:
  application:
    name: servicio-backend-hospital

  datasource:
    url: jdbc:postgresql://localhost:5432/system_hospital
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
